# Greg — Telegram → laptop assistant
# Install with:  pip install -r requirements.txt

requests>=2.31        # Telegram Bot API (long polling) + file downloads
anthropic>=0.40       # Greg's brain (Claude turns your words into actions)
openai>=1.40          # OPTIONAL — Whisper voice-note transcription
pillow>=10.0          # OPTIONAL — screenshots ("send me a screenshot")
pywhatkit>=5.4        # OPTIONAL — hands-free WhatsApp sending (needs WhatsApp Web)

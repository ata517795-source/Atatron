# ── Anthropic API key (REQUIRED — the brain of the scanner) ─────────────────
# Create one at https://platform.claude.com/ → API Keys.
# Locally: copy this file to .env and fill it in, then run `vercel dev`.
# On Vercel: add it under Project → Settings → Environment Variables.
ANTHROPIC_API_KEY=
